.nav-wrapper {
  position: relative;
  height: 50vh;
  margin-bottom: 54px;
}

.nav {
  position: sticky;
  top: 100px;
  display: flex;
  justify-content: space-between;
  font-size: 26px;
  line-height: 1.34;
}
